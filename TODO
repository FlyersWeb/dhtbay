Improve updateSeed
=> Looks like trackers answers are different, so we have to chose only one
=> For forceAll mode, we need to slowdown process because too much requests are sent

Improve categorize script
=> Can we have category based on torrent hash from an API???
